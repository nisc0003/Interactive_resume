---
import Heading from "../components/Heading.astro";
import Button from "./Button.astro";
import Skills from "../components/Skills.astro";
---

<div
  class="bg-bg2 lg:col-span-4 lg:col-start-1 lg:row-start-1 lg:rounded-lg flex flex-col justify-between min-h-screen lg:min-h-0"
>
  <!-- Top Quote Section -->
  <div class="p-6 lg:p-8">
    <Heading
      htmlheading="h2"
      type="div_heading"
      font="display2"
      stylingclasses="text-text-white lg:text-left leading-relaxed"
    >
      "Digital design med fokus på UX, UI & prototyping"
    </Heading>
  </div>

  <!-- Contact & Skills Section -->
  <div class="lg:bg-bg1 rounded-lg p-6 lg:p-8 lg:m-6 lg:mb-8 space-y-6">
    <!-- Contact Heading -->
    <div class="intersect intersect:animate-fade-up animate-duration-700">
      <Heading
        htmlheading="h3"
        type="list_heading"
        font="display2"
        stylingclasses="text-text-white mb-5"
      >
        Kontakt mig
      </Heading>
    </div>

    <!-- Buttons -->
    <div class="flex flex-col gap-3">
      <Button variant="privateMail" />
      <Button variant="workMail" />
      <Button variant="call" />
      <Button variant="download" />
    </div>

    <!-- Skills Component -->
    <Skills />
  </div>
</div>
